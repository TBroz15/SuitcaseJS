export const upFirstChar = (string: string) =>
  string.charAt(0).toUpperCase() + string.slice(1);

export const lowFirstChar = (string: string) =>
  string.charAt(0).toLowerCase() + string.slice(1);
